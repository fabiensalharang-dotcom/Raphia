import appName from '../../app-name.json';

// Le nom commercial est provisoire (CLAUDE.md) : une seule source pour
// cette chaîne, partagée avec la config native (app.config.ts).
export const common = {
  'app.name': appName.value,
  'common.confirm': 'Valider',
};
